dist
node_modules
*.md
# pnpm が管理する lockfile は prettier 整形しない（install のたびに差分が暴れるため）
pnpm-lock.yaml
# Illustrator の ExtendScript（ES3）。prettier はオブジェクトリテラルに末尾カンマ（ES5 構文）を入れるため整形しない
scripts/ai/*.jsx
.claude/skills/edit-icons/scripts/*.jsx
